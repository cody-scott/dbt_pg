dbt_pg:
  outputs:
    dev:
      dbname: TestDB
      host: localhost
      pass: mysecretpassword
      port: 5432
      schema: public
      threads: 1
      type: postgres
      user: postgres
  target: dev